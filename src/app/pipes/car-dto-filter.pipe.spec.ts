import { CarDtoFilterPipe } from './car-dto-filter.pipe';

describe('CarDtoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CarDtoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
